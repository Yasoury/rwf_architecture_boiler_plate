name: sign_in
publish_to: none

environment:
  sdk: ">=3.0.5 <4.0.0"

dependencies:
  component_library:
    path: ../../component_library
  flutter_bloc: ^8.1.5
  intl: ^0.19.0

  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  user_repository:
    path: ../../user_repository
  form_fields:
    path: ../../form_fields
  domain_models:
    path: ../../domain_models

dev_dependencies:
  flutter_test:
    sdk: flutter
  bloc_test: ^9.0.3
  mockito: ^5.2.0
  test: ^1.16.8
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
