name: profile_menu
publish_to: none

environment:
  sdk: ">=3.0.5 <4.0.0"

dependencies:
  component_library:
    path: ../../component_library
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2

  flutter_bloc: ^9.1.0
  user_repository:
    path: ../../user_repository
  domain_models:
    path: ../../domain_models
  rxdart: ^0.28.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.3
  test: ^1.16.8
  flutter_lints: ^6.0.0

flutter:
  generate: true
  uses-material-design: true

